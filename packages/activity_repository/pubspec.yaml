name: activity_repository
publish_to: none

environment:
  sdk: ^3.6.0-216.1.beta

dependencies:
  fav_qs_api_v2:
    path: ../fav_qs_api_v2
  domain_models:
    path: ../domain_models
  key_value_storage:
    path: ../key_value_storage
  doublequotes:
    path: ../../

dev_dependencies:
  lints: ^5.1.0
